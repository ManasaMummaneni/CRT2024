Area of rectangle using functions
#include <stdio.h>
    void area()
    {
        int a,b;
        scanf("%d %d",&a,&b);
        int r=a*b;
        printf("%d",r);
    }
    int main()
    {
        area();
        return 0;
    }

 
