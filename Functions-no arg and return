Area of rectangle using functions(no arguments and return type)
#include <stdio.h>
    int area()
    {
        int l,b;
        scanf("%d %d",&l,&b);
        int r=l*b;
        return r;
    }
       
    int main()
    {
        int k=area();
        printf("%d",k);
    }
